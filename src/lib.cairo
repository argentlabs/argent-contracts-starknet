mod multisig {
    mod argent_multisig;
    mod interface;

    // Structures 

    mod signer_signature;
}

mod library {
    mod account;
    mod array_ext;
    mod asserts;
    mod calls;
    mod erc165;
    mod outside_execution;
    mod upgrade;
    mod version;
}
